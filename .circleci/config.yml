version: 2
jobs:
  build:
    docker:
      - image: circleci/node
    branches:
      only:
        - master
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      # - run: npm run build
      - deploy:
          command: .circleci/deploy.sh
